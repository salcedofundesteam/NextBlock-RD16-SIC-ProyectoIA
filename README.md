## NextBlock

### Installation Guide:

- pip install fastapi (web framework to build APIs)
- pip install uvicorn (server to test and run APIs apps)

#### In order to run the project: uvicorn main:app --reload
